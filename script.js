function scrollToSection(sectionId) {
  document.getElementById(sectionId).scrollIntoView({ behavior: "smooth" });
}

const toggleTheme = () => {
  document.body.classList.toggle("dark-mode");
};

function contactame(sectionId) {
  document.getElementById(sectionId).scrollIntoView({ behavior: "smooth" });
}
